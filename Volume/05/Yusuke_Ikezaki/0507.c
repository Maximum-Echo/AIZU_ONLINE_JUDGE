#include <stdio.h>

int main(void)
{
  int n;

  for(;;){
    scanf("%d", &n);
    if(n==0) break;

    for(j=n;ans[0]!=1;){
      rem=0;
      int ans[30]={0};
      for(i=0;;){
	ans[i]=j-rem;
	rem=n-ans[i];
	if(rem==0) break;
	if(rem>0){
	  
      
